import React from 'react';
import Navbar from '../Components/Navbar';
import Footer from '../Components/Footer';
import FramedPicture from '../Components/CertificatePicture';

const Gallery = () => {
  return (
    <div>
      <Navbar/>
      <FramedPicture/>
      <Footer/>
    </div>
  );
};

export default Gallery;
